# Optimism (Demo site)

This repository currently contains a small demo static site deployed to the `gh-pages` branch so GitHub Pages can serve a live preview.

If you want to deploy your real React/Vite/CRA/Vue app here, push the source files and I will add an Actions workflow or gh-pages setup to publish the build folder.
